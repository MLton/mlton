The files in this directory define the MLton structure within the
context of the Standard ML Basis Library.  Many things are defined to
raise exceptions, but enough is emulated so that we can compile MLton
itself.
